// import { useSelector } from "react-redux";
// import {getContacts} from '../contact-selectors'

const HomeView = () => {

    // const contacts = useSelector(getContacts)
    // console.log(contacts.filter(a => a))
 
    return (<h1>Главная страница</h1>)
}



export default HomeView;